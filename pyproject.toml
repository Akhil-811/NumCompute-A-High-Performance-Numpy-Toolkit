[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "numcompute"
version = "0.2.0"
description = "NumCompute: A modular machine learning framework built from scratch using NumPy and Matplotlib"
authors = [
    { name = "Eruva Akhil" }
]
readme = "README.md"
requires-python = ">=3.8"

dependencies = [
    "numpy>=1.21",
    "matplotlib>=3.5"
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "jupyter>=1.0",
    "matplotlib>=3.5"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["numcompute*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"